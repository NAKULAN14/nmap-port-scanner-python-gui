# nmap-port-scanner-python-gui
This project is a GUI-based port scanner built in Python that uses Nmap to scan a target system for open ports and running services. The application provides an easy interface for users to perform scans without using command-line tools.
